<%= form_with(model: @group, local: true) do |form| %>
  <div class="message">
    <%= form.text_field :name, placeholder: "グループ名を入力" %>
  </div>
  
  <div class="submit">
    <%= form.submit "送信" %>
  </div>
<% end %>