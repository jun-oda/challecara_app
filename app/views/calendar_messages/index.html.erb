<%= form_with(model: @calendar_message, local: true) do |form| %>

  <div class="message">
    <%= form.text_field :message, placeholder: "メッセージを入力" %>
  </div>

  <div class="submit">
    <%= form.submit "送信" %>
  </div>
<% end %>